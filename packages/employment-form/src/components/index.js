import React from "react";
import ReactDOM from "react-dom";
import { Title } from "./../../../components/Title/index";

const Root = document.getElementById("employment-form");

ReactDOM.render(<Title name={"employment-form"} />, Root);
