import React from "react";
import ReactDOM from "react-dom";
import { Title } from "./../../../components/Title/index";

const Root = document.getElementById("biographic-form");

ReactDOM.render(<Title name={"biographic-form"} />, Root);
