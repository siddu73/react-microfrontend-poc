import React from "react";
import ReactDOM from "react-dom";
import { Title } from "./../../../components/Title/index";

const Root = document.getElementById("picture-form");

ReactDOM.render(<Title name={"picture-form"} />, Root);
